#include <iostream>
#include <fstream>
#include <array>
#include <vector>
#include <string>

using namespace std;

int main ()
{
    ifstream file("solution.txt");
    string output;
    array
    while (getline(file, output)) {
        for (int i = 0; i < output.length(); i += 2) {

        }
    }
}
