#include <iostream>
#include <stdlib.h>
#include <fstream>
#include <vector>
#include <string>

using namespace std;

int main ()
{
    fstream f("H:\\Documents\\Assignments\\DATA21.txt");
    string out;
    vector<vector<string[3]>> b;
    vector<string> t;

    while (!f.eof()) {
        getline(f, out);
    }
}
